computational-thinking-assistant/
│
├─ app.py                          ← Flask 应用入口（路由）
├─ config.py                       ← 配置类
├─ requirements.txt                ← Python 依赖
├─ .env                            ← 环境变量
│
├─ services/                       ← 业务逻辑层 ⭐
│   ├─ __init__.py
│   └─ llm_service.py              ← LLM 服务
│
├─ utils/                          ← 工具函数 ⭐
│   ├─ __init__.py
│   └─ validators.py               ← 数据验证
│
├─ templates/                      ← HTML 模板
│   └─ index.html                  ← 主页面
│
└─ static/                         ← 静态资源
    ├─ css/
    │   └─ style.css               ← 样式表
    │
    ├─ js/
    │   ├─ config.js               ← 前端配置 ⭐
    │   ├─ models/
    │   │   └─ ChatModel.js        ← Model（API 调用）
    │   ├─ views/
    │   │   └─ ChatView.js         ← View（DOM 操作）
    │   ├─ controllers/
    │   │   └─ ChatController.js   ← Controller（事件处理）
    │   └─ main.js                 ← 应用入口
    │
    └─ images/


┌─────────────────────────────────────────────────────────────┐
│                    浏览器端 (Browser)                       ·│
│  ┌──────────────────────────────────────────────────────┐   │
│  │            前端 MVC (Frontend MVC)                    │   │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐    │   │
│  │  │   Model    │  │    View    │  │ Controller │    │   │
│  │  │ (API调用)  │  │ (DOM操作)  │  │ (事件处理) │    │   │
│  │  │ChatModel.js│  │ChatView.js │  │ChatCtrl.js │    │   │
│  │  └────────────┘  └────────────┘  └────────────┘    │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                            ↕ HTTP/JSON
┌─────────────────────────────────────────────────────────────┐
│                   服务器端 (Server)                           │
│  ┌──────────────────────────────────────────────────────┐   │
│  │         后端 MVC + 服务层 (Backend MVC + Service)     │   │
│  │                                                        │   │
│  │  ┌─────────────┐     路由层 (Routes)                 │   │
│  │  │   app.py    │  ← 接收请求，路由分发                │   │
│  │  └─────────────┘                                      │   │
│  │         ↓                                              │   │
│  │  ┌─────────────┐     业务逻辑层 (Service Layer)       │   │
│  │  │  Services   │  ← LLM, Auth, Chat 等服务           │   │
│  │  └─────────────┘                                      │   │
│  │         ↓                                              │   │
│  │  ┌─────────────┐     数据访问层 (Model/DAO)           │   │
│  │  │   Models    │  ← User, ChatHistory 等模型         │   │
│  │  └─────────────┘                                      │   │
│  │         ↓                                              │   │
│  │  ┌─────────────┐     数据持久层 (Database)            │   │
│  │  │  Database   │  ← SQLite / MySQL                   │   │
│  │  └─────────────┘                                      │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘